apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: grafana
  name: grafana
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  selector:
    app: grafana