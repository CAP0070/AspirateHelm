apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: prometheus
  name: prometheus
spec:
  ports:
  - name: http
    port: 9090
    targetPort: 0
  selector:
    app: prometheus
  type: ClusterIP