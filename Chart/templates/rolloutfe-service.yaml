apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: rolloutfe
  name: rolloutfe
spec:
  type: {{ .Values.services.rolloutfe.type }}
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: {{ .Values.services.rolloutfe.nodePort }}
  selector:
    app: rolloutfe