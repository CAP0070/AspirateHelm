apiVersion: v1
kind: Service
metadata:
  annotations: {}
  labels:
    app: jaeger
  name: jaeger
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 16686
      targetPort: 16686
  selector:
    app: jaeger